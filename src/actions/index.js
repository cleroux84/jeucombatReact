
export const hitMonster = (payload) => {
    return { type: "HIT_MONSTER", 
              payload}
              
  };

  export const hitBack = (payload) => {
    return { type: "HIT_BACK", 
              payload}
  };
